#!/usr/bin/env bash
#Bash script that displays the time for 12 hours and 59 minuts
hours=0
while [ $hours -le 12 ]
do
  minutes=0
  while [ $minutes -le 60 ]
  do
    echo "Hour: $hours"
    echo $minutes
    minutes=$((minutes + 1))
  done
  hours=$((hours + 1))
done
