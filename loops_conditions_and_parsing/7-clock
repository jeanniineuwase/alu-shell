#!/usr/bin/env bash
# Initialize the hours and minutes variables
hour=0
while true; do
  for minute in $(seq 1 59); do
    echo "Hour: $hour"
    echo "$minute"
  done
  ((hour++))
  if [ "$hour" -eq 13 ]; then
    hour=1
  fi
done
